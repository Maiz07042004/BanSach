@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model BanSachMVC.Models.Order
@using System.Globalization

@{
    ViewBag.Title = "Chi Tiết Đơn Hàng";
}

<h2>@ViewBag.Title</h2>

<div class="card">
    <div class="card-header">
        <h5>Thông Tin Khách Hàng</h5>
    </div>
    <div class="card-body">
        <p><strong>Tên khách hàng:</strong> @Model.CustomerName</p>
        <p><strong>Địa chỉ:</strong> @Model.Address</p>
        <p><strong>Số điện thoại:</strong> @Model.PhoneNumber</p>
        <p><strong>Email:</strong> @Model.Email</p>
        <p><strong>Ghi chú:</strong> @Model.OrtherNotes</p>
        <p><strong>Ngày đặt hàng:</strong> @Model.OrderDate.ToString("dd/MM/yyyy HH:mm")</p>
        <p><strong>Tình trạng:</strong> @Model.Status</p>
    </div>
</div>

<br />

<div class="card">
    <div class="card-header">
        <h5>Thông Tin Đơn Hàng</h5>
    </div>
    <div class="card-body">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Tên sách</th>
                    <th>Số lượng</th>
                    <th>Đơn giá</th>
                    <th>Tổng giá</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.OrderItems)
                {
                    <tr>
                        <td>@item.Book.Title</td>
                        <td>@item.Quantity</td>
                        <td>@item.UnitPrice.ToString() đ</td>
                        <td>@(item.Quantity*item.UnitPrice) đ</td>
                    </tr>
                }
            </tbody>
        </table>
        <h5 class="text-right mt-3">
            <strong>Tổng số tiền: </strong> @Model.TotalAmount.ToString("N0", new CultureInfo("vi-VN")) ₫
        </h5>
    </div>
</div>

<div class="mt-3">
    <a href="@Url.Action("Index", "QuanLyDonHang")" class="btn btn-secondary">Quay lại danh sách</a>
</div>

